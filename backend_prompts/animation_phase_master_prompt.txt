# COSCHOOL MANIM MASTER PROMPT v5.0 — CONCISE EDITION

---

INPUT:
"""
[PASTE RAW TRANSCRIPT HERE]
"""

METADATA:
Title: <> | Subject: <> | Grade: <> | Duration: <> | Type: <Concept|Micro|Problem>

---

## ARCHITECTURE

THREE LAYERS: LAYOUT (positions) → ANIMATION (self.play) → AUDIO (bookmarks)
TWO TRACKS: TRACK A (transcript→audio script→voiceover) | TRACK B (transcript→storyboard→positions→animations)
Bookmarks bridge A→B via wait_until_bookmark(). Tracks never cross.

---

## PHASE 2 PIPELINE CONTRACT

This prompt is used by the Phase 2 app one script at a time. Treat the current INPUT as exactly one script/transcript unit unless the app explicitly says otherwise.

Application orchestration order:
1. Generate the storyboard package for every detected script first, one API call per script.
2. Only after all storyboards are complete, generate Python code for every script, one API call per script.
3. Code generation must use the EXISTING STORYBOARD PACKAGE supplied for the same script only.
4. Never merge multiple SCRIPT blocks into one storyboard or one Python file.
5. Storyboard mode must not output Python code.
6. Code mode must output raw production-ready Python code only; no markdown fences, no prose, no storyboard rewrite.

---

## DESIGN SYSTEM

```
COLORS:  LAVENDER_BG="#E7E5F3"  PURPLE="#7464CE"  ORANGE_HL="#FF9302"  PALE_PURPLE="#9495D7"  WHITE="#FFFFFF"
FONTS:   Text()→font="Poppins"   MathTex()→tex_template=TexFontTemplates.gnu_freesans_tx
SIZES:   Title=52  Badge=28  Question=26  Formula=36  Step=28  Answer=40(ORANGE)  Dim=22  Unknown=36  Legend=20  Caption=22  Summary=26  Concept=26
SLIDES:  Title=PURPLE bg, white text, topic only | Content=LAVENDER_BG, badge top-left | Summary=LAVENDER_BG, bullets only (NO card boxes)
```

Grade 6-7: stroke≥3, font≥32, concrete shapes, Types 1-2 anchors mandatory
Grade 8-9: ValueTracker, graphs, number lines, Types 3-4 mandatory
Grade 10+: TransformMatchingTex, formal notation, Types 4-5 mandatory, VSF critical

---

## LAYOUT GRID

Frame: 14.22×8.00 | Safe: x∈[-6.11,+6.11] y∈[-3.25,+3.25]

```
SLOT_HEADER     = UL corner, buff=0.3          SLOT_TITLE      = ORIGIN
SLOT_QUESTION   = UP*2.5                       SLOT_FIGURE_C   = ORIGIN
SLOT_FIGURE_R   = RIGHT*3.0                    SLOT_STEPS      = UP*2.0+LEFT*3.5
SLOT_STEPS_SOLO = UP*2.0+LEFT*1.0              SLOT_LEGEND     = DR corner, buff=0.4
SLOT_CONCEPT    = UP*0.5                       SLOT_CAPTION    = DOWN*2.5
SLOT_ANCHOR_C   = ORIGIN                       SLOT_ANCHOR_B   = DOWN*1.2
SLOT_SUMM_1/2/3 = UP*1.5 / ORIGIN / DOWN*1.5
```

COEXISTENCE: Title=SLOT_TITLE only | Question=HEADER+QUESTION+FIGURE_C | Solution=HEADER+FIGURE_R+STEPS+LEGEND | Concept=HEADER+ANCHOR_C+CONCEPT | Summary=SUMM_1+2+3 only

Dimension arrows: OUTSIDE shape, labels OUTSIDE arrows, buff≥0.3
Figure transition Q→S: animate figure_group to SLOT_FIGURE_R (never redraw)

---

## ALL IRON LAWS

**1. BACKGROUND IS NOT A WIPE:** `camera.background_color=X` does NOT clear. Always FadeOut all→wait→change bg. Use `clear_and_transition()`.

**2. ONE SLIDE TYPE = ONE VOICEOVER BLOCK:** Title=silent(NO voiceover) | Concept/Question/Solution/Summary=each gets own voiceover block.

**3. TITLE CARD SILENT PRE-ROLL:** Title is silent-only. self.wait(2.0). Greeting moves to FIRST content segment voiceover. ❌Never self.wait(3.0). ❌Never voiceover in show_title().

**4. NEVER `or` ON NUMPY VECTORS:** ❌`arg or default` ✅`arg if arg is not None else default`

**5. NEVER POSITION HIGHLIGHTS BY MathTex.width ARITHMETIC:** Split MathTex into separate objects for selective highlighting.

**6. NEVER >4 MathTex OBJECTS IN ONE HORIZONTAL ROW:** Split wide rows into stacked sub-rows. Max 6.0 units width for step zone.

**7. STEPMANAGER IS BLIND TO SUB-ROWS:** Group multi-line items into single VGroup for add_step() (Solution A), or phase-split (Solution B). ❌Never next_to() sub-rows between add_step() calls.

**8. EVERY MOB NEEDS EXACTLY ONE FADEOUT OWNER:** Either in mgr.steps (owned by fadeout_all) or in active_mobs (owned by clear_and_transition). Never both without cleanup.

**9. MANDATORY PRE-FADEIN OVERLAP CHECK:** Always use safe_fadein()/safe_create() for layout elements. ❌Never bare self.play(FadeIn(mob)).

**10. EVERY VOICEOVER SENTENCE MUST HAVE A VISUAL:** No blank screens. Every sentence→≥1 visible element.

**11. VISUAL-STEP FIDELITY:** Every narrated sub-operation = one visual change. Never collapse two operations into one frame. Show intermediate forms before simplified. Use make_compound_fraction() for nested fractions (never make_fraction with embedded \frac). Every simplification needs ReplacementTransform showing both forms.

**12. FIGURE COMPONENTS BYPASS OVERLAP:** Geometric sub-components (shapes inside shapes, dots on curves, chords inside circles, markers at vertices, tick marks on segments, perpendiculars, altitudes, diagonals) use figure_create()/figure_fadein() — NOT safe_create()/safe_fadein(). Test: "Would moving this break the math?" YES→figure_*(). NO→safe_*(). Use centralized builder function computing all positions from one anchor point.

**13. EVERY FIGURE MUST BE SELF-EXPLANATORY:** Students must understand the figure without audio. Every named length→dimension label. Every right angle→square marker. Equal segments→tick marks. Tangent points→dots. Unknown→"?" or variable. All labels font_size≥20, min gap 0.3 units between labels. Labels use safe_fadein() (they're layout elements, not geometry).

**MANDATORY SCENE CLEAR SEQUENCE:**
```
STEP 1: self.play(*[FadeOut(m) for m in active_mobs], run_time=0.8)
STEP 2: self.wait(0.2)
STEP 3: self.camera.background_color = NEW_COLOR
STEP 4: self.wait(0.1)
STEP 5: # NOW safe to FadeIn new content
```

**STEPMANAGER SAFE LIMITS:** (32,0.4)→3 | (28,0.3)→4 | (24,0.25)→5 | (20,0.2)→6. Compound fractions count as 1.5-2× height.

---

## OUTPUT STEPS

### STEP 0 — DIRECTOR'S NOTE (≤120 words)
Topic, goal, pacing plan, 3-5 emphasis terms.
CATEGORY: 1=Geometric 2=Proportional 3=Algebraic
ANCHOR: INCLUDED/SKIPPED + reason

**Visual Anchor Inventory** — for EVERY sentence: Sentence#, Anchor Type(1-6), Element, Slot
**VSF Inventory** — for every math derivation: list each sub-operation → planned visual frame
**Solution Row Pre-Check** — per step: object count, width safe?, phase count, compound fractions
**Phase Split Plan** — if total steps > SAFE_LIMITS

### STEP 1 — RAW TRANSCRIPT SUMMARY
Numbered verbatim sentences grouped by segment. NO changes.

### STEP 2 — AUDIO SCRIPT (TRACK A)
✅ Symbols→spoken words, add commas/dashes for pacing, break long sentences (10-25 words)
✅ Embed `<bookmark mark="bk_xxx"/>` 2-3 words BEFORE trigger
✅ ALL fractions→"X over Y" (never "half"/"quarter")
✅ Greeting = FIRST sentence of first CONTENT segment (not title)
✅ Every visual anchor + every VSF sub-operation gets its own bookmark
❌ Never add/remove words, never paraphrase

### STEP 3 — STORYBOARD + POSITION TABLE (TRACK B)
**Storyboard columns:** Scene|Visuals|Anchor Type(G1/D2/T3/A4/S5/X6)|VSF Frame?(YES/INTER/SIMPLIFY/—)|Narration|On-Screen|Focus|Bookmark
One visual action per row. Every algebraic sub-operation = separate row.

**Position Table:** collision check before code. No shared slots, steps x≤-0.5, figure x≥+0.5, all in safe area, stack≤5.0 units.

### STEP 4 — PYTHON CODE
Production-ready Manim CE v0.19.0. See templates and helpers below.

### STEP 5 — OVERLAP AUDIT
Run mentally before outputting code. Full checklist at end of prompt.

### STEP 6 — RENDER
```bash
manim render -ql file.py Scene   # preview
manim render -qh file.py Scene   # high quality
manim render -qk file.py Scene   # 4K
```

---

## CODE TEMPLATES

### STRUCTURE
```python
class TopicScene(VoiceoverScene):
    def construct(self):
        self._setup_tts()
        self.show_title()
        self.show_concept()
        self.show_question()
        self.show_solution()
        self.show_summary()
```
Each segment = one method. Each has local active_mobs=[]. Each ends with clear_and_transition(). construct() has ZERO self.play().

### TITLE (silent — NO voiceover)
```python
def show_title(self):
    active_mobs = []
    self.camera.background_color = PURPLE
    self.wait(0.1)
    topic = Text("Topic", font="Poppins", font_size=52, color=WHITE, weight=BOLD)
    topic.move_to(ORIGIN)
    self.play(FadeIn(topic), run_time=0.8)
    active_mobs.append(topic)
    self.wait(2.0)
    clear_and_transition(self, active_mobs, LAVENDER_BG)
    active_mobs.clear()
```

### CONCEPT (greeting is FIRST sentence here)
```python
def show_concept(self):
    active_mobs = []
    badge = create_heading_badge("Concept")
    self.play(FadeIn(badge), run_time=0.5)
    active_mobs.append(badge)
    with self.voiceover(text='<bookmark mark="bk_greeting"/>...') as tracker:
        self.wait_until_bookmark("bk_greeting")
        card = make_concept_card("Hook text", position=UP*2.0)
        safe_fadein(self, card, active_mobs, name="card")
        # Visual anchors at their bookmarks...
        # FIGURE elements → figure_create()/figure_fadein()
        # TEXT labels → safe_fadein()
    clear_and_transition(self, active_mobs, LAVENDER_BG)
    active_mobs.clear()
```

### SOLUTION (StepManager always, phase split when needed)
```python
def show_solution(self):
    active_mobs = []
    badge = create_heading_badge("Solution")
    self.play(FadeIn(badge), run_time=0.5)
    active_mobs.append(badge)
    # Figure at SLOT_FIGURE_R → figure_create() for geometry
    mgr = StepManager(self, start_anchor=UP*2.2+LEFT*3.4, font_size=24, buff=0.28)
    with self.voiceover(text='...') as tracker:
        # Steps via mgr.add_step(), active_mobs.append()
        # Phase split: mgr.fadeout_all() + remove from active_mobs + wait(0.2) + fresh StepManager
        # Final answer in ORANGE_HL
    clear_and_transition(self, active_mobs, LAVENDER_BG)
    active_mobs.clear()
```

### SUMMARY (bullets only, NO card boxes)
```python
def show_summary(self):
    active_mobs = []
    badge = create_heading_badge("Summary")
    self.play(FadeIn(badge), run_time=0.5)
    active_mobs.append(badge)
    with self.voiceover(text='...') as tracker:
        for i, (txt, pos) in enumerate(zip(bullets, [UP*1.5, ORIGIN, DOWN*1.5])):
            self.wait_until_bookmark(f"bk_sum{i+1}")
            b = make_bullet_point(txt, position=pos, font_size=24)
            safe_fadein(self, b, active_mobs, name=f"bullet_{i+1}")
    self.wait(0.6)
    clear_and_transition(self, active_mobs, LAVENDER_BG)
    active_mobs.clear()
```

---

## HELPER QUICK REFERENCE

```
math_obj(r"...")                              Standard math expression
make_fraction("num","den")                    Simple fraction (1 bar)
make_compound_fraction(num_mob, den_mob)      Nested fraction (2+ bars)
create_heading_badge("Solution")              Heading badge
create_dimension(start, end, "10 cm")         Dimension arrow+label
make_concept_card(txt, position)              Concept card (WITH box)
make_bullet_point(txt, position)              Summary bullet (NO box)
make_legend([("l","= length")])               Variable legend
safe_fadein(scene, mob, active_mobs)          Layout element + overlap check
safe_create(scene, mob, active_mobs)          Layout shape + overlap check
figure_fadein(scene, mob, active_mobs)        Figure dot/marker (NO overlap check)
figure_create(scene, mob, active_mobs)        Figure shape/line (NO overlap check)
clear_and_transition(self, mobs, COLOR)       Slide transition
StepManager(self, start_anchor, font_size, buff)  Multi-step algebra
mgr.replace_last(old, new)                    VSF simplification transition
make_cancel_line(mob)                         Strike-through for cancellation
make_highlight_box(mob)                       Highlight box around mob
make_arrow_annotation(mob, text, dir)         Annotation arrow
```

---

## BOILERPLATE + ALL HELPER CODE

```python
import os
import urllib.request
import manimpango
from dotenv import load_dotenv
from manim import *
from manim_voiceover import VoiceoverScene
from manim_voiceover.services.openai import OpenAIService

load_dotenv()

LAVENDER_BG = "#E7E5F3"
PURPLE      = "#7464CE"
ORANGE_HL   = "#FF9302"
PALE_PURPLE = "#9495D7"

def _setup_poppins():
    base_dir  = os.path.dirname(os.path.abspath(__file__))
    fonts_dir = os.path.join(base_dir, ".fonts")
    os.makedirs(fonts_dir, exist_ok=True)
    base_url = "https://raw.githubusercontent.com/google/fonts/main/ofl/poppins/"
    fonts = {
        "Poppins-Regular.ttf":  base_url + "Poppins-Regular.ttf",
        "Poppins-Bold.ttf":     base_url + "Poppins-Bold.ttf",
        "Poppins-Italic.ttf":   base_url + "Poppins-Italic.ttf",
        "Poppins-SemiBold.ttf": base_url + "Poppins-SemiBold.ttf",
    }
    for fname, url in fonts.items():
        path = os.path.join(fonts_dir, fname)
        if not os.path.exists(path):
            try: urllib.request.urlretrieve(url, path)
            except Exception as e: print(f"Could not download {fname}: {e}"); continue
        try: manimpango.register_font(path)
        except Exception: pass

_setup_poppins()

import manim_voiceover.tracker as _vt
_orig = _vt.VoiceoverTracker.time_until_bookmark
_FAILED = []
def _safe_tub(self, mark, buff=0.0, limit=None):
    try: return _orig(self, mark, buff, limit)
    except Exception:
        _FAILED.append(mark); print(f"WARNING: bookmark '{mark}' not found"); return 0.0
_vt.VoiceoverTracker.time_until_bookmark = _safe_tub
import atexit
def _report():
    if _FAILED: print(f"\nFAILED BOOKMARKS: {_FAILED}")
atexit.register(_report)

TTS_INSTRUCTIONS = """
You are a warm, patient math teacher. Tone: friendly, calm, never rushed.
Pace: moderate-to-slow. Honor commas, dashes, ellipses as pacing marks.
Slow down on variables and formulas. Emphasize shape names and final answers.
FRACTIONS: When you see "X over Y", say exactly "X over Y".
Never say "X-Yths" or common names like "one half" or "three quarters".
Read the script EXACTLY. No filler. No improvisation.
"""

# ── HELPERS ──────────────────────────────────────────────────────

def create_heading_badge(text_str):
    t = Text(text_str, font="Poppins", font_size=28, color=WHITE, weight=BOLD)
    bg = RoundedRectangle(corner_radius=0.2, width=t.width+0.6, height=t.height+0.3,
                          fill_color=PURPLE, fill_opacity=1, stroke_width=0)
    bg.move_to(t)
    return VGroup(bg, t).to_corner(UL, buff=0.3)

def create_dimension(start, end, label_str, direction=DOWN, buff=0.3):
    arrow = DoubleArrow(start=start, end=end, color=PURPLE, stroke_width=2, tip_length=0.2, buff=0)
    label = Text(label_str, font="Poppins", font_size=22, color=PURPLE)
    label.next_to(arrow.get_center(), direction, buff=0.15)
    return VGroup(arrow, label)

def create_unknown(position):
    return Text("?", font="Poppins", font_size=36, color=ORANGE_HL, weight=BOLD).move_to(position)

def math_obj(tex_str, color=PURPLE, font_size=36):
    return MathTex(tex_str, tex_template=TexFontTemplates.gnu_freesans_tx, color=color, font_size=font_size)

def _make_cosec_template():
    base = TexFontTemplates.gnu_freesans_tx
    t = TexTemplate(tex_compiler=base.tex_compiler, output_format=base.output_format,
                    preamble=base.preamble, placeholder_text=base.placeholder_text)
    t.add_to_preamble(r"\DeclareMathOperator{\cosec}{cosec}")
    return t
COSEC_TEMPLATE = _make_cosec_template()

def math_obj_cosec(tex_str, color=PURPLE, font_size=36):
    return MathTex(tex_str, tex_template=COSEC_TEMPLATE, color=color, font_size=font_size)

def make_fraction(num_tex, den_tex, font_size=36, color=PURPLE):
    """Simple fraction — ONE bar. ❌Never pass string containing \\frac"""
    n = MathTex(num_tex, tex_template=TexFontTemplates.gnu_freesans_tx, font_size=font_size, color=color)
    d = MathTex(den_tex, tex_template=TexFontTemplates.gnu_freesans_tx, font_size=font_size, color=color)
    w = max(n.width, d.width) + 0.3
    bar = Line(LEFT*w/2, RIGHT*w/2, color=color, stroke_width=2.5)
    n.next_to(bar, UP, buff=0.15); d.next_to(bar, DOWN, buff=0.15)
    return VGroup(n, bar, d)

def make_compound_fraction(num_mob, den_mob, font_size=36, color=PURPLE):
    """Nested fraction — TWO+ bars. Outer bar thicker(3.5) and wider(+0.6)."""
    if isinstance(num_mob, str):
        num_mob = MathTex(num_mob, tex_template=TexFontTemplates.gnu_freesans_tx, font_size=font_size, color=color)
    if isinstance(den_mob, str):
        den_mob = MathTex(den_mob, tex_template=TexFontTemplates.gnu_freesans_tx, font_size=font_size, color=color)
    for mob in [num_mob, den_mob]:
        if isinstance(mob, VGroup) and len(mob) >= 3 and isinstance(mob[1], Line):
            mob[1].set_stroke(width=2.0)
    w = max(num_mob.width, den_mob.width) + 0.6
    outer_bar = Line(LEFT*w/2, RIGHT*w/2, color=color, stroke_width=3.5)
    num_mob.next_to(outer_bar, UP, buff=0.25); den_mob.next_to(outer_bar, DOWN, buff=0.25)
    return VGroup(num_mob, outer_bar, den_mob)

def make_overline_label(tex_str, font_size=36, color=PURPLE):
    lbl = MathTex(tex_str, tex_template=TexFontTemplates.gnu_freesans_tx, font_size=font_size, color=color)
    bar = Line(lbl.get_corner(UL)+UP*0.08, lbl.get_corner(UR)+UP*0.08, color=color, stroke_width=2.5)
    return VGroup(lbl, bar)

def safe_math(tex_str, color=PURPLE, font_size=36, stroke_width=None):
    obj = math_obj(tex_str, color=color, font_size=font_size)
    if stroke_width: obj.set_stroke(width=stroke_width)
    return obj

def make_legend(entries, position=DR, buff=0.4):
    rows = []
    for var_tex, def_str in entries:
        v = MathTex(var_tex, tex_template=TexFontTemplates.gnu_freesans_tx, font_size=20, color=ORANGE_HL)
        d = Text(def_str, font="Poppins", font_size=20, color=PURPLE)
        rows.append(VGroup(v, d).arrange(RIGHT, buff=0.1))
    content = VGroup(*rows).arrange(DOWN, aligned_edge=LEFT, buff=0.25)
    bg = RoundedRectangle(corner_radius=0.15, width=content.width+0.4, height=content.height+0.3,
                          fill_color=WHITE, fill_opacity=0.85, stroke_color=PALE_PURPLE, stroke_width=1.0)
    bg.move_to(content)
    g = VGroup(bg, content)
    if position is not None: g.to_corner(position, buff=buff)
    return g

def make_concept_card(text_str, position=ORIGIN, font_size=26, max_chars=55):
    if len(text_str) > max_chars:
        words = text_str.split(); lines, cur = [], ""
        for w in words:
            if len(cur)+len(w)+1 <= max_chars: cur += (" " if cur else "")+w
            else: lines.append(cur); cur = w
        if cur: lines.append(cur)
        text_str = "\n".join(lines)
    txt = Text(text_str, font="Poppins", font_size=font_size, color=PURPLE)
    bg = RoundedRectangle(corner_radius=0.2, width=min(txt.width+0.8,10.5), height=txt.height+0.4,
                          fill_color=WHITE, fill_opacity=0.85, stroke_color=PALE_PURPLE, stroke_width=1.5)
    bg.move_to(position); txt.move_to(bg.get_center())
    return VGroup(bg, txt)

def make_bullet_point(text_str, position=ORIGIN, font_size=26, max_chars=52):
    if len(text_str) > max_chars:
        words = text_str.split(); lines, cur = [], ""
        for w in words:
            if len(cur)+len(w)+1 <= max_chars: cur += (" " if cur else "")+w
            else: lines.append(cur); cur = w
        if cur: lines.append(cur)
        text_str = "\n".join(lines)
    dot = Text("\u2022", font="Poppins", font_size=font_size+4, color=ORANGE_HL)
    txt = Text(text_str, font="Poppins", font_size=font_size, color=PURPLE)
    row = VGroup(dot, txt).arrange(RIGHT, buff=0.25, aligned_edge=UP)
    row.move_to(position)
    return row

def make_grid_overlay(w, h, cell=0.5, color=PALE_PURPLE):
    cols, rows_n = int(w/cell), int(h/cell)
    cells = VGroup()
    for r in range(rows_n):
        for c in range(cols):
            sq = Rectangle(width=cell, height=cell, fill_color=color, fill_opacity=0.15,
                          stroke_color=PURPLE, stroke_width=0.5)
            sq.move_to(RIGHT*(c+0.5)*cell + DOWN*(r+0.5)*cell)
            cells.add(sq)
    cells.move_to(ORIGIN)
    return cells

def make_tape_diagram(total, active, cw=1.2, ch=0.8):
    blocks = VGroup()
    for i in range(total):
        is_a = i < active
        b = Rectangle(width=cw, height=ch, color=PURPLE, stroke_width=2.5,
                      fill_color=ORANGE_HL if is_a else PALE_PURPLE, fill_opacity=0.8 if is_a else 0.3)
        blocks.add(b)
    blocks.arrange(RIGHT, buff=0)
    if blocks.width > 8.0: blocks.scale(8.0/blocks.width)
    top_br = Brace(VGroup(*list(blocks)[:active]), UP, color=ORANGE_HL)
    top_lbl = math_obj(str(active), color=ORANGE_HL, font_size=28).next_to(top_br, UP, buff=0.1)
    bot_br = Brace(blocks, DOWN, color=PURPLE)
    bot_lbl = math_obj(str(total), font_size=28).next_to(bot_br, DOWN, buff=0.1)
    return {"blocks": blocks, "diagram": VGroup(blocks, top_br, top_lbl, bot_br, bot_lbl)}

def make_number_line(x_range=(-5,5,1), length=10, include_numbers=True):
    return NumberLine(x_range=x_range, length=length, color=PURPLE, stroke_width=2.5,
                      include_numbers=include_numbers, font_size=22, label_direction=DOWN,
                      decimal_number_config={"color": PURPLE})

def make_arrow_annotation(mob, text_str, direction=UP, color=ORANGE_HL):
    arrow = Arrow(mob.get_edge_center(direction)+direction*0.8, mob.get_edge_center(direction)+direction*0.15,
                  color=color, stroke_width=2.5, tip_length=0.15)
    label = Text(text_str, font="Poppins", font_size=20, color=color)
    label.next_to(arrow, direction, buff=0.1)
    return VGroup(arrow, label)

def make_highlight_box(mob, color=ORANGE_HL, buff=0.1):
    return SurroundingRectangle(mob, color=color, stroke_width=2.5, buff=buff, corner_radius=0.1)

def make_cancel_line(mob, color=ORANGE_HL):
    return Line(mob.get_corner(DL)+LEFT*0.05+DOWN*0.05, mob.get_corner(UR)+RIGHT*0.05+UP*0.05,
                color=color, stroke_width=2.5)

def clear_and_transition(scene, active_mobs, new_bg, ft=0.8, buf=0.2, settle=0.1):
    if active_mobs: scene.play(*[FadeOut(m) for m in active_mobs], run_time=ft)
    scene.wait(buf); scene.camera.background_color = new_bg; scene.wait(settle)

SAFE_L, SAFE_R = -6.11, 6.11
SAFE_T, SAFE_B = 3.25, -3.25

def check_safe_margins(mob, name="obj"):
    ok = True
    if mob.get_left()[0]<SAFE_L or mob.get_right()[0]>SAFE_R: ok=False
    if mob.get_top()[1]>SAFE_T or mob.get_bottom()[1]<SAFE_B: ok=False
    if not ok: print(f"MARGIN WARNING: {name}"); clamp_to_safe_area(mob)
    return ok

def clamp_to_safe_area(mob):
    sx, sy = 0, 0
    if mob.get_left()[0]<SAFE_L: sx=SAFE_L-mob.get_left()[0]
    elif mob.get_right()[0]>SAFE_R: sx=SAFE_R-mob.get_right()[0]
    if mob.get_bottom()[1]<SAFE_B: sy=SAFE_B-mob.get_bottom()[1]
    elif mob.get_top()[1]>SAFE_T: sy=SAFE_T-mob.get_top()[1]
    if sx or sy: mob.shift(RIGHT*sx+UP*sy)
    return mob

def check_y_gap(new_mob, existing_mobs, min_gap=0.3, name="new_mob"):
    for mob in existing_mobs:
        if isinstance(mob, VGroup) and len(mob)==0: continue
        nb, nt = new_mob.get_bottom()[1], new_mob.get_top()[1]
        mb, mt = mob.get_bottom()[1], mob.get_top()[1]
        if nb < mt and nt > mb:
            new_mob.shift(UP*(mt+min_gap-nb))
        elif nb >= mt and (nb-mt) < min_gap:
            new_mob.shift(UP*(min_gap-(nb-mt)))
    return new_mob

def has_overlap(a, b, margin=0.15):
    return (a.get_left()[0]-margin < b.get_right()[0] and a.get_right()[0]+margin > b.get_left()[0] and
            a.get_bottom()[1]-margin < b.get_top()[1] and a.get_top()[1]+margin > b.get_bottom()[1])

def resolve_overlaps(new_mob, active_mobs, name="new"):
    for mob in active_mobs:
        if isinstance(mob, VGroup) and len(mob)==0: continue
        if has_overlap(new_mob, mob):
            shift_y = mob.get_bottom()[1]-new_mob.get_top()[1]-0.2
            new_mob.shift(DOWN*abs(shift_y))
            if new_mob.get_bottom()[1]<SAFE_B:
                new_mob.shift(UP*abs(shift_y))
                new_mob.shift(RIGHT*(mob.get_right()[0]-new_mob.get_left()[0]+0.3))
    clamp_to_safe_area(new_mob)
    return new_mob

def safe_fadein(scene, mob, active_mobs, name="obj", run_time=0.7):
    check_safe_margins(mob, name); check_y_gap(mob, active_mobs, name=name)
    resolve_overlaps(mob, active_mobs, name)
    scene.play(FadeIn(mob), run_time=run_time); active_mobs.append(mob)
    return mob

def safe_create(scene, mob, active_mobs, name="obj", run_time=1.0):
    check_safe_margins(mob, name); resolve_overlaps(mob, active_mobs, name)
    scene.play(Create(mob), run_time=run_time); active_mobs.append(mob)
    return mob

def figure_fadein(scene, mob, active_mobs, name="obj", run_time=0.7):
    """Figure sub-component FadeIn — margin check only, NO overlap check."""
    check_safe_margins(mob, name)
    scene.play(FadeIn(mob), run_time=run_time); active_mobs.append(mob)
    return mob

def figure_create(scene, mob, active_mobs, name="obj", run_time=1.0):
    """Figure sub-component Create — margin check only, NO overlap check."""
    check_safe_margins(mob, name)
    scene.play(Create(mob), run_time=run_time); active_mobs.append(mob)
    return mob

class StepManager:
    LIMITS = {(32,0.4):3, (28,0.3):4, (24,0.25):5, (20,0.2):6}
    def __init__(self, scene, start_anchor=None, font_size=28, buff=0.3):
        self.scene=scene; self.steps=[]; self.fs=font_size; self.buff=buff
        self.max=self.LIMITS.get((font_size,buff),4)
        self.anchor = start_anchor if start_anchor is not None else (UP*2.0+LEFT*3.5)

    def add_step(self, mob, run_time=0.7):
        if len(self.steps)>=self.max: print(f"WARNING: StepManager at limit ({self.max}).")
        if self.steps:
            mob.next_to(self.steps[-1], DOWN, aligned_edge=LEFT, buff=self.buff)
            self.scene.play(*[s.animate.set_opacity(0.4) for s in self.steps], FadeIn(mob), run_time=run_time)
        else:
            mob.move_to(self.anchor); self.scene.play(FadeIn(mob), run_time=run_time)
        self.steps.append(mob)
        if mob.get_bottom()[1]<SAFE_B: print("WARNING: step below safe area")
        return mob

    def replace_last(self, old_mob, new_mob, run_time=1.0):
        new_mob.move_to(old_mob)
        self.scene.play(ReplacementTransform(old_mob, new_mob), run_time=run_time)
        if old_mob in self.steps: self.steps[self.steps.index(old_mob)] = new_mob
        return new_mob

    def highlight_last(self):
        if self.steps: self.scene.play(self.steps[-1].animate.set_color(ORANGE_HL), run_time=0.5)

    def get_all(self): return VGroup(*self.steps)

    def fadeout_all(self, rt=0.8):
        if self.steps: self.scene.play(*[FadeOut(s) for s in self.steps], run_time=rt); self.steps.clear()
```

---

## OVERLAP AUDIT CHECKLIST (Step 5)

**POSITION:** □ All elements in safe area □ No shared slots □ Steps x≤-0.5, figure x≥+0.5 □ Stack≤5.0/phase □ Steps≤SAFE_LIMITS/phase

**FIGURE GEOMETRY:** □ All figure sub-components use figure_create()/figure_fadein() □ NO figure part uses safe_create()/safe_fadein() □ All positions from centralized builder □ Shapes concentric where needed □ Chords tangent at correct y □ Perpendiculars connect correct points □ Right-angle markers at correct vertices □ Tick marks on correct segments

**SELF-EXPLANATORY LABELS:** □ Every measurement labeled □ Right angles marked □ Equal segments have ticks □ Unknown marked with ?/variable □ Labels use safe_fadein() □ No two labels overlap □ font_size≥20 □ Student understands figure without audio

**VISUAL ANCHORS:** □ Every sentence has anchor type □ Every anchor has bookmark □ No blank screens □ Type 6 only when Types 1-5 impossible

**VSF:** □ Every sub-operation has own bookmark+visual □ No two ops share one transition □ Compound fracs use make_compound_fraction() □ Simplifications show both forms with animation

**AUDIO:** □ Greeting in first content voiceover □ No voiceover in show_title() □ All fractions→"X over Y" □ Bookmarks 2-3 words before trigger

**ANIMATION:** □ Layout elements use safe_fadein()/safe_create() □ Figure elements use figure_create()/figure_fadein() □ No bare self.play(FadeIn()) for layout □ All fractions via helpers □ FadeOut all at segment end □ No `or` on numpy vectors

**STEPMANAGER:** □ No sub-rows via next_to() between add_step() □ Multi-line grouped into VGroup □ Phase split when > SAFE_LIMITS □ Phase cleanup: fadeout_all()+remove active_mobs+wait(0.2)

**SLIDES:** □ Title=silent(2.0s) □ One voiceover per slide type □ clear_and_transition() every transition □ active_mobs.clear() after transition

---

## OUTPUT ORDER

0. Director's Note (category+anchor+inventories+phase plan)
1. Raw Transcript Summary (verbatim)
2. Audio Script (bookmarks, greeting in first content segment)
3. Storyboard + Position Table
4. Python Code
5. Overlap Audit
6. Render Commands
